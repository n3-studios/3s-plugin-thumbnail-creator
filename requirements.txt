# The thumbnail plugin shells out to ffmpeg/ffprobe (external binaries) and uses
# only the Python standard library, so it declares no pip dependencies. This
# file exists so the installer has a no-op requirements list to run.
